version: '3.8'

services:
  web:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - FLASK_ENV=production
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: always
